# 安全说明

- 不要提交或分享真实的网盘 Token、Cookie、Client Secret、Apple 登录信息或验证码。
- App 运行时的账号凭证存放在 iOS Keychain；项目没有自建文件中转服务。
- 文档预览会先写入每次请求独有的临时文件，完整校验后再原子发布到 App 系统缓存，并按账号与文件版本隔离；一小时前的预览半成品会清理，有版本信息的缓存最长保留 7 天，没有版本信息的缓存最多复用 15 分钟，缓存总量按 512 MB 回收，单个预览文件也不得超过 512 MB。主动下载的文件写入 `Documents/Downloads`，并可通过系统“文件”与分享面板访问。敏感文件使用完后请从本机删除。
- 下载记录文件只保存任务状态、文件名和本地路径，并通过临时文件原子替换写入 `Documents/.juyun` 隐藏状态目录，单个日志上限为 10 MB。启动时按修改时间选择临时日志或主日志的最新有效快照；临时日志并非更新时以主日志为准，并拒绝损坏的顶层结构。最多保留 500 条历史并优先保留待完成任务。暂停下载所需的 URL、Header、有效期与 iOS 断点数据加密存入 Keychain，并在继续、取消、删除账号或移除任务后清理。
- App 恢复任务时会验证下载链接有效期、目标路径、Header、断点长度和本机文件状态；断点文件必须始终匹配同一个聚云管理路径。每次下载运行使用独立代次，同一路径在所有旧运行结束前保持引用计数占用。下载完成、重启恢复与已知大小的预览完成时都会核对精确目标路径和文件大小。损坏、被篡改、串任务或截断的文件不会被标记为完成，失败、取消和无法恢复的半成品会从 `Downloads` 清理。
- 所有发往网盘的 JSON、下载和上传请求都会拒绝包含换行、非法控制字符、空名称、大小写重复名称、超长值或数量异常的 Header，避免歧义或损坏凭证进入原生网络栈。Header 单值 64 KB、整组 256 KB 的限制按 UTF-8 实际字节计算；下载地址还会拒绝原始控制字符和内嵌用户名密码。
- 较长的 Token、Cookie 和断点数据会拆成小于 2 KB 的 Keychain 分片，避免 iOS 对大值的历史限制；分片不会写入普通文件，批量读写与孤立断点清理采用受控并发。账号 ID 与 Keychain 键名规则一致，账号索引最多 500 项；索引与凭证的写入、更新和删除会串行执行，并在 Keychain 失败时回滚。保存成功后界面使用实际持久化副本，避免界面、索引和凭证不一致。
- 服务商 JSON 响应按 UTF-8 实际字节默认限制为 20 MB，整份下载暂停状态限制为 220 KB，原生单次内存读取限制为 64 MB；非法数值、超大缓冲区和畸形顶层响应会在进入系统接口前被拒绝。
- “保存并测试连接”会先使用临时凭证测试，只有成功后才替换 Keychain 中的原账号；并发刷新同一账号 Token 时只允许一个刷新请求写回。后台 Token 或空间 ID 回写还会比较 Keychain 最新凭证快照，陈旧请求只能同步新值，不能覆盖用户刚保存的配置。
- 传输页“清理记录”和单项“移除记录”只删除完成或失败的历史与断点状态，不删除本机文件或网盘原文件；只有明确点击“删除文件”才会删除聚云管理的本机下载。
- 原生 AES、RSA、HMAC 与哈希代码只用于各网盘要求的上传签名、秒传校验和分片校验，不接收或处理网盘登录密码；HMAC 编码枚举和 RSA 公钥长度会在进入系统加密接口前校验。
- 原生文件接口和上传源只接受无远程主机、查询参数、片段或空字符的本机文件 URI；分片复制还会拒绝源文件与目标文件相同的请求。上传每个分片前后会复核源文件大小与修改时间，临时分片必须存在且精确等于声明字节数；哈希、证明码和服务商要求的二次校验结束后，也会在创建上传或秒传任务前再次复核源文件。
- 依赖审计在 Expo 57 的 Metro 构建链中报告 `image-size <= 2.0.2` 的恶意图片解析拒绝服务问题。截至本版本制作时，上游尚未发布公告所需的 `2.0.3` 修复版。该包用于开发/打包阶段读取项目图片，不会进入聚云的网盘请求或播放器运行路径。只使用仓库内可信图片，不要把不可信 ICNS/JXL/HEIF 文件加入项目；上游发布兼容修复后应立即升级 Expo/Metro 依赖。
- `xcode` 的旧版间接 `uuid` 依赖已通过 npm override 固定到修复版 11.1.1，并通过 Expo Doctor 与 iOS bundle 校验。

发现凭证意外进入仓库时，不要只删除文件：应立即在对应网盘开放平台撤销并重新签发凭证。
